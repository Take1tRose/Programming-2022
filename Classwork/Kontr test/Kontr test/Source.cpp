#include <iostream>
using namespace std;
int main()
{ //перша задача

	/*int h1;
	int m1;
	int h2;
	int m2;
	cout << "Enter your hour" << endl;
	cin >> h1;
	cout << "Enter your minute" << endl;
	cin >> m1;
	cout << "Enter second hour" << endl;
	cin >> h2;
	cout << "Enter second minute" << endl;
	cin >> m2;

	int d1 = h1 * 60;
	int d2 = h2 * 60;
	int s1 = d1 + m1;
	int s2 = d2 + m2;
	int s = s2 - s1;
	if (0 < h1 && 25 > h1) {
		if (0 < m1 && 60 > m1) {
			if (0 < h2 && 25 > h2) {
				if (0 < m2 && 60 > m2) {
					if (0 > s) {
						cout << s * -1;
					}
					else {
						cout << s;
					}
				}
				else {
					"pomilka";
				}
			}
			else {
				cout << "pomilka";
			}
		}
		else {
			cout << "pomilka";
		}
	}
	else {
		cout << "pomilka";
	}*/
    
    
	//друга задача

    /*int size;
    int num_array[1000];
    int sum = 0;
    for (int i = 0; true; i++)
    {
        cout << "Input random number x" << i + 1 << ": " << endl;
        cin >> num_array[i];
        cout << "Input 0 to end" << endl;
        if (num_array[i] == 0)
        {
            size = i;
            break;
        }
    }
    int mult = num_array[0];
    for (int i = 1; i < size; i++)
    {
        mult *= num_array[i];
    }
    cout << "Geometric mean: " << pow(mult, 1.00 / size);*/
}