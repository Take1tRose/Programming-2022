#include <iostream>
#include <time.h>
using namespace std;
int main() {
	/*double arr[]={2,5,7,2,5};
	double sum = 0;
	double count = 0;
	double average;
	cout << "Here is numders"<<endl;
	for (const double &n: arr) {
		cout << n << " " << endl;
		sum += n;
		++count;
	}
	cout << "\nTheir Sum: " << sum << endl;
	average = sum / count;
	cout << "Their Average: " << average << endl;*/



	/*const int size = 10;
	int mas[size];
	for (int i = 0; i < size;) {
		mas[i] = rand() % 99 + 1;
		if (mas[i] % 2 != 0) {
			i++;
		}
	}
	for (int j = 0; j < size; j++) {
		cout << mas[j] << " " << endl;
	}*/



	/*const int n=10;
	cout << "Enter " << n << " numbers " << endl;
	int arr[n];
	double min_value;
	double sum = 0;
	double count = 0;
	double average;
	for (int i = 0; i < n; i++) {
		cin >> arr[i];
		min_value = arr[0];
		if (min_value > arr[i]) {
			min_value = arr[i];
		}
		sum += n;
		++count;
	}
	average = sum / count;
	cout << "Their average: " << average << endl;
	cout << "Min value: " << min_value;*/
}